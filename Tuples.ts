// 

// const user: string [] = ["hc" ];

let User : [string , number , boolean];
User = ["hc"  , 2 , true];



// tuples are like , how u define the array i sequence string , number , boolean we have assgin them value in that order if not assign then it will occur an error 